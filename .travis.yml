language: node_js

# Safelist
branches:
  only:
    - master

jobs:
  include:
    - stage: install
      script: npm install
    - stage: build
      script: npm run compile
    - stage: test
      script: npm run test